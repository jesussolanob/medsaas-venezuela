/**
 * lib/supabase-client.ts — NEUTRALIZADO
 *
 * Este módulo era un wrapper de createBrowserClient de @supabase/ssr.
 * No tiene consumidores activos en el proyecto (grep: 0 resultados).
 * Se conserva el archivo para que cualquier import residual no rompa la compilación,
 * pero NO exporta un cliente Supabase real.
 *
 * FASE futura: eliminar este archivo por completo cuando se confirme que
 * ninguna rama experimental lo utiliza.
 */

// Intencional: sin exports. Los consumidores deben migrar a backendGet/backendPost
// de @/lib/api-client.server o a @/lib/supabase/client si aún requieren Supabase.
export {};
