'use client'
// Componente vacío — el flujo de aprobaciones fue eliminado.
// Conservado solo para que el import en page.tsx no rompa builds antiguos.
export default function ApprovalsClient() { return null }
