# Arquitectura

Documentación de diseño del sistema: capas (Next.js App Router, API routes, Supabase), autenticación por roles, multi-tenant, RLS y flujos críticos (booking, citas, consultas).

**Próximos temas sugeridos:** mapa de rutas `app/`, diagrama de roles (`admin` / `doctor` / `patient`), integración Supabase Auth + `profiles`.

Consulta [CLAUDE.md](../../CLAUDE.md) para el estado actual del producto.
